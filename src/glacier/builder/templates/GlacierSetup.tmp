package glacier.builder.templates;

import com.badlogic.gdx.graphics.PerspectiveCamera;
import com.badlogic.gdx.utils.Array;

import de.fatox.metagine.graphics.BufferRenderer;
import de.fatox.metagine.graphics.gbuffer.BufferTexture;

public class GlacierSetup {
	<shader_constants>;
	public GlacierSetup(PerspectiveCamera cam) {
		BufferRenderer br = new BufferRenderer(cam, <targetCount>);
		Array<BufferTexture> texs;
		<addBuffers>;
	}
}
